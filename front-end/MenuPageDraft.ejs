<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="MenuPageDraftStyles.css">
    <title>Hamburger Menu Overlay</title>
</head>
<body>
<div class="menu-wrap">
    <input type="checkbox" class="toggler">
    <div class="main-menu-icon"><div></div></div>
    <div class="menu">
        <div>
            <div>
                <ul>
                    <li><a href="/">HOME PAGE</a></li>
                    <li><a href="#">ABOUT</a></li>
                    <li>
                        <% if (!loggedIn) { %>
                            <a href="/login">LOGIN</a>
                        <% } %>
                    </li>
                    <li><a href="/characteristics">CONTENT</a></li>
                    <li>
                        <% if (adminUser) { %>
                            <a href="/poll">POLL</a>
                        <% } %>
                    </li>
                    <li><% if (adminUser) { %>
                            <a href="/admin/register">ADD NEW ADMIN</a>
                        <% } %>
                    </li>
                    <li>
                        <% if (loggedIn) { %>
                            <a href="/logout">LOGOUT</a>
                        <% } %>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>

<header class="showcase">
    <div class="container showcase-inner">
        <h1>HEADING</h1>
        <div id="username">
            <% if (username) { %>
                <p>Hi, <%= username %>!</p>
            <% } %>
        </div>
        <p>there will be some text, I don't really know which one,<br> but there will definitely be some shit</p>
        <a href="/characteristics" class="button">READ MORE</a>
    </div>
</header>
</body>

</html>